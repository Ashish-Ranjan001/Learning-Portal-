<!-- 
<div id="carouselExample" class="carousel slide">
  <div class="carousel-inner">
    <div class="carousel-item active">
      <img src="evs1.png" class="d-block w-100" alt="...">
    </div>
    <div class="carousel-item">
      <img src="evs.jpg" class="d-block w-100" alt="...">
    </div>
    
  </div>
  <button class="carousel-control-prev" type="button" data-bs-target="#carouselExample" data-bs-slide="prev">
    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
    <span class="visually-hidden">Previous</span>
  </button>
  <button class="carousel-control-next" type="button" data-bs-target="#carouselExample" data-bs-slide="next">
    <span class="carousel-control-next-icon" aria-hidden="true"></span>
    <span class="visually-hidden">Next</span>
  </button>
</div>
<div class="container">
  <h1>Evalueserve</h1>
  
  <div class="login-options">
    <button routerLink="/app-login" class="login-button">User Login</button>
    <button routerLink="/app-login" class="login-button">Admin Login</button>
  </div>
</div> -->
<header>
  <!-- Intro settings -->
  <style>
    /* Default height for small devices */
    #intro-example {
      height: 400px;
    }

    /* Height for devices larger than 992px */
    @media (min-width: 992px) {
      #intro-example {
        height: 600px;
      }
    }
  </style>

  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg bg-body">
    <div class="container-fluid">
      <button
        class="navbar-toggler"
        type="button"
        (click)="backgroundImageExample2.toggle()"
        aria-controls="backgroundImageExample2"
        aria-expanded="false"
        aria-label="Toggle navigation"
      >
        <i class="fas fa-bars"></i>
      </button>
      <div
        mdbCollapse
        #backgroundImageExample2="mdbCollapse"
        class="collapse navbar-collapse"
        id="backgroundImageExample2"
      >
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item active">
            <a class="nav-link" aria-current="page" href="">Home</a>
          </li>

          <li class="nav-item">
            <a
              class="nav-link"
              href="https://www.evalueserve.com/speak-to-expert/"
              >Help</a
            >
          </li>
        </ul>
      </div>
    </div>
  </nav>
  <!-- Navbar -->

  <!-- Background image -->
  <div
    id="intro-example"
    class="p-5 text-center bg-image"
    style="background-image: url('evs5.jpg')"
  >
    <div class="mask" style="padding: 50px 20px">
      <div
        class="d-flex flex-column justify-content-center align-items-center h-100"
      >
        <div class="text-white text-center">
          <h1 class="mb-4 display-4 fw-bold animate-text">Evalueserve</h1>
          <h5 class="mb-4 animate-text">
            At Evalueserve, we empower businesses <br />
            with advanced, tech-driven insights that enhance <br />
            strategic decisions and fuel impactful results.
          </h5>
         
          <div class="login-options mt-4">
            <a
              class="btn btn-light btn-lg m-2 rounded-pill px-4 shadow-lg animate-btn"
              routerLink="/user-login"
            >
              User Login
            </a>
            <a
              class="btn btn-light btn-lg m-2 rounded-pill px-4 shadow-lg animate-btn"
              routerLink="/admin-login"
            >
              Admin Login
            </a>
          </div>

          <a
            class="btn btn-light btn-lg m-2 rounded-pill px-4 shadow-lg animate-btn"
            href="https://www.evalueserve.com/about/"
            target="_blank"
            role="button"
          >
            About Us
          </a>
        </div>
      </div>
    </div>
  </div>
  <!-- Background image -->
</header>

import { CommonModule } from '@angular/common';
import { Component } from '@angular/core';
import { FormsModule, ReactiveFormsModule } from '@angular/forms';
import { Router, NavigationEnd, RouterOutlet, RouterModule } from '@angular/router';
import { MdbCollapseModule } from 'mdb-angular-ui-kit/collapse';

@Component({
  selector: 'app-landingpage',
  templateUrl: './landingpage.component.html',
  imports: [CommonModule,ReactiveFormsModule,FormsModule,MdbCollapseModule,RouterModule],
  styleUrls: ['./landingpage.component.css']
})
export class LandingpageComponent {
  pageTitle = 'Evalueserve'; // Default title

  constructor(private router: Router) {
    this.router.events.subscribe(event => {
      if (event instanceof NavigationEnd) {
        this.updateTitle(event.url);
      }
    });
  }

  updateTitle(url: string) {
    if (url.includes('user-login')) {
      this.pageTitle = 'User Login';
    } else if (url.includes('admin-login')) {
      this.pageTitle = 'Admin Login';
    } else {
      this.pageTitle = 'Evalueserve';
    }
  }
}
/* Base button styling */
.login-options a {
  transition: all 0.3s ease-in-out;
}

/* User Login Button - Hover Effect */
.login-options a[routerLink="/user-login"]:hover {
  background-color: #5c2385; /* Deep Purple */
  color: white;
  transform: scale(1.05);
}

/* Admin Login Button - Hover Effect */
.login-options a[routerLink="/admin-login"]:hover {
  background-color: #9b7021; /* Teal */
  color: white;
  transform: scale(1.05);
}

/* About Us Button - Hover Effect */
a[href="https://www.evalueserve.com/about/"]:hover {
  background-color: #ffa500; /* Orange */
  color: white;
  transform: scale(1.05);
}
/* Navbar styling */
.navbar {
  background-color: rgba(0, 0, 0, 0.8); /* Slightly darker for contrast */
  padding: 10px 20px;
}

.navbar-nav {
  display: flex;
  text-align: left;
  
  width: 100%;
}

.nav-link {
  font-size: 18px;
  font-weight: bold;
  
  transition: all 0.3s ease-in-out;
}

/* Home Link Hover Effect */
.nav-link[href=""] {
  color: rgb(123, 134, 138);
}

.nav-link[href=""]:hover {
  color: #905b92; /* Dark Violet */
  text-decoration: underline;
  transform: scale(1.1);
}

/* Help Link Hover Effect */
.nav-link[href="https://www.evalueserve.com/speak-to-expert/"] {
  color: rgb(123, 134, 138);
}

.nav-link[href="https://www.evalueserve.com/speak-to-expert/"]:hover {
  color:  #905b92;  
  text-decoration: underline;
  transform: scale(1.1);
}
html, body {
  height: 100vh; /* Full viewport height */
  overflow: hidden; /* Disable scrolling */
  margin: 0;
  padding: 0;
}

/* Ensure the main content fills the screen */
#intro-example {
  height: 100vh; /* Make the background fill the full screen */
  background-size: cover;
  background-position: center;
  background-repeat: no-repeat;
}

/* Center everything inside the mask */
.mask {
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100%;
  text-align: center;
}

/* Adjust the navbar to fit within the viewport */
.navbar {
  position: absolute;
  top: 0;
  width: 100%;
  z-index: 100;
}

/* Ensure buttons stay within view */
.login-options {
  display: flex;
  justify-content: center;
  gap: 20px;
}
/* Make text more visible */
h1{
    color: rgb(75, 12, 69);
}

/* Adjust the mask */


/* Styling for the h5 text */
h5.animate-text {
  font-size: 22px;
  font-weight: 600;
  color: rgb(59, 50, 59);/* Ensures bright visibility */
  line-height: 1.6; /* Improves readability */
  letter-spacing: 0.5px; /* Slight spacing for elegance */
  animation: fadeInUp 1s ease-in-out;
}

/* Smooth fade-in effect */
@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
/* Fade-in-up animation for the heading */
h1.animate-text {
  font-size: 48px;
  font-weight: 700;
  
  animation: fadeInUp 1.2s ease-in-out;
}

/* Smooth fade-in effect */
@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(30px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

